type: object
additionalProperties: false
properties:
  title:
    type: string
    description: The title of the thread
    pattern: ^[0-9a-z]+$
    maxLength: 32
  body:
    type: string
    description: The body of the thread
    pattern: ^[0-9a-z]+$
    maxLength: 32